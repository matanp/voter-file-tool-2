# Copy to .env and fill in real values. No secrets in this file.
# See docs/LOCAL_DEVELOPMENT.md for full local dev setup.

# Database (required for Docker setup; see scripts/README.md)
# POSTGRES_USER=your_user
# POSTGRES_PASSWORD=your_password
# POSTGRES_DB=your_database
# POSTGRES_PORT=5432
# POSTGRES_PRISMA_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/${POSTGRES_DB}

# Or point at a remote/prod DB (no Docker):
# POSTGRES_PRISMA_URL=postgresql://user:password@host:5432/database

# Auth (required; validated in src/env.js)
# AUTH_SECRET=
# AUTH_GOOGLE_ID=
# AUTH_GOOGLE_SECRET=

# NextAuth base URL (optional; defaults to http://localhost:3000 in dev)
# NEXTAUTH_URL=http://localhost:3000

# Report-server (for PDF generation). Local dev: http://localhost:8080
# PDF_SERVER_URL=http://localhost:8080

# Webhook secret (must match report-server WEBHOOK_SECRET)
# WEBHOOK_SECRET=

# R2 / S3-compatible storage (use a dev bucket; same vars in report-server)
# R2_ACCOUNT_ID=
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_BUCKET_NAME=

# Ably (realtime report status)
# ABLY_API_KEY=

# Optional: Vercel, Sentry, etc. are used when set.
