# 1.R.18 Phase 1 Tests and Validations (P2)

**Status:** Open
**Priority:** P2 — Medium
**Effort:** 0.5 day
**Depends on:** [1.2 CommitteeMembership Model](1.2-committee-membership-model.md), [1.4 Seat Model](1.4-seat-model.md)

## Summary

Improve type safety and structure: (1) in validations/committee, if Prisma/type imports appear after type declarations that use them, move imports to the top; (2) in committeeMappingHelpers.test, replace `as never` with the correct input type (e.g. CommitteeWithMemberships) so the test fixture is type-checked.

## Acceptance Criteria

- [ ] In validations/committee: if the file has local types that depend on Prisma types with imports after those definitions, relocate the import block to the top of the file. Verify structure matches finding before changing.
- [ ] In committeeMappingHelpers.test: import or define the correct input type for `mapCommitteesToReportShape`, declare the test input as that typed array, and remove the `as never` cast.

## Implementation Notes

- **Files:** [apps/frontend/src/lib/validations/committee.ts](apps/frontend/src/lib/validations/committee.ts) (if structure exists), [apps/report-server/src/__tests__/committeeMappingHelpers.test.ts](apps/report-server/src/__tests__/committeeMappingHelpers.test.ts).

## Related

- [FINDINGS_AND_RESOLUTION_REGISTER.md](../FINDINGS_AND_RESOLUTION_REGISTER.md) (Findings 17, 18)
